# Midea XH-202615 competition dependencies
# Install: pip install -r requirements.txt
#
# VoiceDetective base deps (torch, speechbrain, etc.) are assumed already
# installed from the repo root requirements.txt.

funasr>=1.0.0
modelscope>=1.9.0

# Audio (likely already installed)
soundfile
librosa
numpy
torch
torchaudio

# Optional speaker-verification backend: gate.embedding_backends=["wavlm"]
transformers

# Optional: faster hub download in China
# huggingface_hub
